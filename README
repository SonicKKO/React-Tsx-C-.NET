
## Описание
Микросервисная архитектура на C# .NET с клиентом на React + TypeScript.

## Структура
- `C#(.NET)/`: серверная часть на C# .NET с двумя микросервисами
- `ReactTsx/`: клиентская часть на React TSX

## Установка

### Серверная часть
1. Убедитесь, что установлен .NET SDK.
2. Перейдите в папку `C#(.NET)/`.
3. Запустите команду `dotnet run` для запуска микросервисов.

### Клиентская часть
1. Перейдите в папку `ReactTsx/`.
2. Установите зависимости: `npm install`.
3. Запустите клиент: `npm start`.
